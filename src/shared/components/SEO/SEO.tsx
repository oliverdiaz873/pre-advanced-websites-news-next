'use client';

interface SEOProps {
  title?: string;
  description?: string;
  imageUrl?: string;
  articleUrl?: string;
  type?: 'website' | 'article';
  datePublished?: string;
  authorName?: string;
}

const SEO = (_props: SEOProps) => null;

export default SEO;
